defaults:
  linkConfig:
    create: true
    force: true
link:
  ~/.profile: profile
  ~/.vimrc:
    path: vim/vimrc
  ~/.bashrc:
  ~/.bash_logout:
  ~/.gitconfig:
  ~/.Xmodmap:
  ~/.Xresources:
  ~/.xinitrc:
  ~/.xprofile:

